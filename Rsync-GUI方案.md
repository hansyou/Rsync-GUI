# Rsync-GUI 软件方案

## 项目概述

Rsync-GUI 是一个轻量级 Web 应用，通过浏览器界面管理 rsync 备份任务，技术栈为 Python 3 + Flask + Flask-SocketIO + APScheduler + SQLite。

## 已实现功能

### 本地任务

- 任务的创建、编辑、克隆、删除
- 手动执行与强制停止，WebSocket 实时推送执行日志
- 定时调度（cron 表达式），支持多任务并行执行
- 保存前命令预览确认
- 源目录多个目标同步
- 演习模式（--dry-run），新建时默认开启
- 排除规则，新建时默认填入 `.Trash-*` `.DS_Store`
- 任务列表备份与还原（JSON 格式，同时导出/导入本地和远程任务）

### 远程任务

- 与本地任务相同的基础功能
- SSH 远程同步（自动拼接 `-e ssh` 参数）
- 主机、用户、端口从路径中自动解析（`user@host:port:/path`）
- 支持自定义 SSH 私钥路径

### 界面

- 本地任务与远程任务左右分栏，独立管理
- 日志弹窗（深色终端风格，含停止/清空按钮）
- ⓘ 悬浮提示图标
- 小屏自适应（≤768px 切换为上下排列）

```
┌──────────────────────────────────────────────────────┐
│ Rsync-GUI                     [备份] [还原]          │
├──────────────────────────────┬───────────────────────┤
│ 本地任务         [＋ 新建]   │ 远程任务   [＋ 新建]  │
│ ┌──────────────────────┐    │ ┌─────────────────┐    │
│ │ 任务卡片              │    │ │ 任务卡片         │    │
│ │ 任务卡片              │    │ │ 任务卡片         │    │
│ └──────────────────────┘    │ └─────────────────┘    │
└──────────────────────────────┴───────────────────────┘
```
